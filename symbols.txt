# ARTIK KULLANILMIYOR
# ---------------------
# Eski akış (halalterminal.com API) bu dosyadan sembol okuyordu. Yeni akış
# (SEC EDGAR + kendi AAOIFI algoritmamız) evreni scripts/build.mjs içindeki
# loadUniverse() ile S&P 500 bileşen CSV'sinden alır; bu dosyaya bakmaz.
#
# Kapsamı genişletmek için build.mjs > loadUniverse() değiştirilir.
